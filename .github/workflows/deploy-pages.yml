name: Deploy to GitHub Pages

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - run: npm install

      - name: Build
        run: npm run build

      - name: SPA fallback (client-side routing, guide + reference app)
        run: |
          cat > publish/404.html <<'EOF'
          <!doctype html>
          <html lang="en">
            <head><meta charset="UTF-8" /></head>
            <body>
              <script>
                (function () {
                  var base = '/house-style/';
                  var isApp = location.pathname.indexOf(base + 'app/') === 0;
                  var target = location.origin + base + (isApp ? 'app/index.html' : 'index.html');
                  fetch(target).then(function (r) { return r.text(); }).then(function (html) {
                    document.open();
                    document.write(html);
                    document.close();
                  });
                })();
              </script>
            </body>
          </html>
          EOF

      - uses: actions/upload-pages-artifact@v5
        with:
          path: publish
          retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
